# Linear Regression Gradient Descent

A from-scratch implementation of linear regression trained with gradient descent using NumPy.

Generates a synthetic dataset with a known relationship (w=3, b=2), then learns those parameters by minimizing mean squared error over 200 epochs. Prints loss and parameter updates every 20 epochs so you can watch the model converge.
